# Core FastAPI and web server
fastapi==0.115.14
uvicorn[standard]==0.35.0
python-multipart==0.0.20

openai>=2.16.0

# Azure Document Intelligence
azure-ai-documentintelligence>=1.0.0
azure-core>=1.29.0

# Environment variables
python-dotenv==1.1.1

# Excel file generation
openpyxl>=3.1.0
pandas>=2.0.0

# PDF manipulation
pypdf>=3.0.0
pdf2image>=1.16.0
pymupdf>=1.23.0
# Drake Automation dependencies
pywinauto>=0.6.8
pyautogui>=0.9.50
psutil>=7.1.3

# Drake Service (Celery + Redis)
celery[redis]>=5.3.0
redis>=5.0.0
httpx>=0.27.0

# PostgreSQL + ORM
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0

# Transitive dependencies (automatically installed with above packages):
# - starlette (FastAPI dependency)
# - pydantic, pydantic_core (FastAPI/pydantic)
# - typing_extensions (pydantic)
# - anyio, sniffio (async dependencies)
# - h11, httpcore, httpx (HTTP client libraries)
# - certifi, charset-normalizer, idna, urllib3 (HTTP/SSL)
# - click (uvicorn CLI)
